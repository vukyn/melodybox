# secrets — copied from include/config.h.example
include/config.h

# PlatformIO build output
.pio/
.vscode/

# Claude Code agent-local scratch (subagent memory, etc.)
# Machine-local Claude state — worktrees and caches — EXCEPT agent-memory,
# which is knowledge about this repo and has to travel with it.
.claude/*
!.claude/agent-memory/
